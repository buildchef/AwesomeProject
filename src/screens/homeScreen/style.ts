import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    fundo: {
        alignItems: "center"
    },
    titulo: {
        textAlign: "center",
        fontSize: 27
    },
    botao: {
        backgroundColor: "#000",
        height: 25,
        width: 300,
        borderRadius: 50,
        marginTop: 50
    },
    textoBotao: {
        color: "#ffffff",
        textAlign: "center",
        fontSize: 16
    }
});

export default styles;