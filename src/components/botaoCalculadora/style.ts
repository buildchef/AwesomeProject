import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    fundoBotao: {
        borderRadius: 20,
        padding: 6,
        width: 80,
        alignItems: "center",
        borderColor: "#778096",
        borderWidth: 5,
        marginRight: 10
    },
    textoBotao: {
        color: "#ffff",
        fontSize: 50,
        fontWeight: "bold"
    }
});

export default style;